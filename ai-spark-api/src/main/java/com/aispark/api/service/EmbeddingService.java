package com.aispark.api.service;

import org.springframework.stereotype.Service;

@Service
public class EmbeddingService {
    public String processEmbedding(String input) {
        // Embedding işleme mantığı
        return "Processed Embedding: " + input;
    }
}
